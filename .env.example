# Phase 0 defines no required environment variables.
# Add documented, non-secret examples here when integrations are implemented.
# Never commit API keys, robot credentials, patient data, or evidence-store secrets.
